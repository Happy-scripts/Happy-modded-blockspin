-- NEBULA HUB | Block Spin - PARTE 1
-- =============================================================

if not game:IsLoaded() then repeat task.wait() until game:IsLoaded() end
if not (game.PlaceId == 104715542330896 or game.PlaceId == 97556409405464) then return end

pcall(function()
    local RS = game:GetService("ReplicatedStorage")
    local TransitionModule = require(RS.Modules.Game.UI.TransitionUI)
    local old_transition = TransitionModule.transition
    TransitionModule.transition = function(p_in, p_wait, p_out, noLogo)
        if old_transition then return old_transition(p_in, p_wait, p_out, noLogo) end
    end
end)

pcall(function()
    local RS = game:GetService("ReplicatedStorage")
    local CharCreator = require(RS.Modules.Game.CharacterCreator.CharacterCreator)
    if CharCreator.start then CharCreator.start = function(...) while true do task.wait(1) end end end
    if CharCreator.load_page then local old = CharCreator.load_page CharCreator.load_page = function(...) return old(...) end end
    if CharCreator.initiate then local old = CharCreator.initiate CharCreator.initiate = function(...) return old(...) end end
end)

pcall(function() if getgenv then getgenv().identifyexecutor = nil end end)
pcall(function() if getfenv then getfenv().identifyexecutor = nil end end)

local ReplicatedStorage = game:GetService("ReplicatedStorage")
local UserInputService = game:GetService("UserInputService")
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")

local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

_G.NetModule = require(ReplicatedStorage.Modules.Core.Net)
_G.RagdollModule = require(ReplicatedStorage.Modules.Game.Ragdoll)
_G.VehicleModule = require(ReplicatedStorage.Modules.Game.VehicleSystem.Vehicle)
_G.CharModule = require(ReplicatedStorage.Modules.Core.Char)
_G.SprintModule = require(ReplicatedStorage.Modules.Game.Sprint)

local Character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
local Humanoid = Character:WaitForChild("Humanoid")

LocalPlayer.CharacterAdded:Connect(function(newChar)
    Character = newChar
    Humanoid = newChar:WaitForChild("Humanoid")
end)

local function permanentLoop(interval, fn)
    task.spawn(function()
        local waitTime = interval or 0.25
        while true do
            local ok = pcall(fn)
            if not ok then task.wait(math.max(waitTime, 0.35)) else task.wait(waitTime) end
        end
    end)
end

local function safeLoadUrl(url)
    local ok, res = pcall(function() return game:HttpGet(url) end)
    if ok and res then
        local ok2, fn = pcall(loadstring, res)
        if ok2 and fn then return fn() end
    end
    return nil
end

local WindUI = safeLoadUrl("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua")
local Icons = safeLoadUrl("https://raw.githubusercontent.com/Footagesus/Icons/main/Main.lua")
if not WindUI then return end
if Icons and Icons.SetIconsType then Icons.SetIconsType("lucide") end

_G.NebulaWindow = WindUI:CreateWindow({
    Icon = "rbxassetid://74826351036046", Title = "NEBULA HUB | Block Spin",
    Author = "by Nebula.lua", Folder = "nebula's", Size = UDim2.fromOffset(480, 360),
    Transparent = true, Theme = "Dark", Resizable = true, SideBarWidth = 165, OpenButton = { Enabled = false }
})

if CoreGui:FindFirstChild("NebulaHubButton") then CoreGui.NebulaHubButton:Destroy() end
local ScreenGui = Instance.new("ScreenGui", CoreGui)
ScreenGui.Name = "NebulaHubButton" ScreenGui.IgnoreGuiInset = true

local ImageButton = Instance.new("ImageButton", ScreenGui)
ImageButton.BackgroundColor3 = Color3.fromRGB(12, 14, 22)
ImageButton.Position = UDim2.new(0.5, -22, 0, 10) ImageButton.Size = UDim2.new(0, 45, 0, 45)
ImageButton.Image = "rbxassetid://74826351036046"

local UICorner = Instance.new("UICorner", ImageButton) UICorner.CornerRadius = UDim.new(0, 8)
local UIStroke = Instance.new("UIStroke", ImageButton) UIStroke.Color = Color3.fromRGB(0, 229, 255) UIStroke.Thickness = 2.5

local dragging, dragStart, startPos, hasDragged, totalDistance = false, nil, nil, false, 0
local isOpen = true

ImageButton.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true dragStart = input.Position startPos = ImageButton.Position hasDragged = false totalDistance = 0
    end
end)
UserInputService.InputChanged:Connect(function(input)
    if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        local delta = input.Position - dragStart
        totalDistance = math.abs(delta.X) + math.abs(delta.Y)
        if totalDistance > 5 then hasDragged = true end
        ImageButton.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)
UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        if dragging then
            dragging = false
            if not hasDragged or totalDistance <= 5 then
                isOpen = not isOpen
                if isOpen then _G.NebulaWindow:Open() else _G.NebulaWindow:Close() end
            end
        end
    end
end)

-- TAB GENERAL
local MainTab = _G.NebulaWindow:Tab({ Title = "GENERAL", Icon = "globe" })
MainTab:Section({ Title = "Player Modifier:" })

local BankBalance = MainTab:Button({ Title = "🏦 Bank Balance", Desc = "N/A" })
local HandBalance = MainTab:Button({ Title = "💸 Hand Balance", Desc = "N/A" })

permanentLoop(0.6, function()
    local hand = tonumber(PlayerGui.TopRightHud.Holder.Frame.MoneyTextLabel.Text:match("%$(%d+)")) or 0
    local bank = 0
    for _, v in ipairs(PlayerGui:GetDescendants()) do
        if v:IsA("TextLabel") and string.find(v.Text, "Bank Balance") then bank = tonumber(v.Text:match("%$(%d+)")) or 0 end
    end
    BankBalance:SetDesc('<b><font color="#00FF00">$' .. bank .. "</font></b>")
    HandBalance:SetDesc('<b><font color="#00f2ff">$' .. hand .. "</font></b>")
end)

local SpeedAmount, EnabledSpeed = 35, false
permanentLoop(0.25, function()
    if EnabledSpeed and Humanoid and Humanoid.Parent then
        _G.NetModule.send("set_sprinting_1", true)
        if _G.SprintModule and _G.SprintModule.sprinting then _G.SprintModule.sprinting.set(true) end
        Humanoid:SetAttribute("TargetWalkSpeed", SpeedAmount)
        Humanoid.WalkSpeed = SpeedAmount
    end
end)

MainTab:Toggle({
    Title = "Walk Speed", Flag = "Walk Speed", Icon = "check", Value = false,
    Callback = function(v)
        EnabledSpeed = v
        if not v and Humanoid then Humanoid:SetAttribute("TargetWalkSpeed", nil) Humanoid.WalkSpeed = 16 _G.NetModule.send("set_sprinting_1", false) end
    end
})
MainTab:Slider({ Title = "Speed Value", Step = 1, Value = { Min = 8, Max = 45, Default = 35 }, Callback = function(v) SpeedAmount = v end })

local EnabledFlyJump, lastFlyJump = false, 0
MainTab:Toggle({ Title = "Fly Jump", Flag = "Fly", Value = false, Callback = function(v) EnabledFlyJump = v end })
UserInputService.JumpRequest:Connect(function()
    if not EnabledFlyJump then return end
    local hrp = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    if hrp and (tick() - lastFlyJump >= 0.12) then
        lastFlyJump = tick()
        hrp.AssemblyLinearVelocity = Vector3.new(hrp.AssemblyLinearVelocity.X, 55, hrp.AssemblyLinearVelocity.Z)
    end
end)

-- TAB COMBAT
local CombatTab = _G.NebulaWindow:Tab({ Title = "COMBAT", Icon = "swords" })
CombatTab:Section({ Title = "GUN MODS" })

_G.GunModSettings = { Enabled = false, accuracy = 1, range = 1000, Recoil = 0, fire_rate = 0.05, reload_time = 0.1, automatic = true }
CombatTab:Toggle({ Title = "Enable Gun Mods", Flag = "enable_gun_mods", Value = false, Callback = function(v) _G.GunModSettings.Enabled = v end })
CombatTab:Slider({ Title = "Fire Rate", Step = 0.01, Value = { Min = 0.01, Max = 1, Default = 0.05 }, Callback = function(v) _G.GunModSettings.fire_rate = v end })
CombatTab:Slider({ Title = "Recoil", Step = 0.1, Value = { Min = 0, Max = 10, Default = 0 }, Callback = function(v) _G.GunModSettings.Recoil = v end })

RunService.Heartbeat:Connect(function()
    if not _G.GunModSettings.Enabled then return end
    local tool = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Tool")
    if tool then
        pcall(function()
            tool:SetAttribute("accuracy", _G.GunModSettings.accuracy)
            tool:SetAttribute("range", _G.GunModSettings.range)
            tool:SetAttribute("Recoil", _G.GunModSettings.Recoil)
            tool:SetAttribute("fire_rate", _G.GunModSettings.fire_rate)
            tool:SetAttribute("automatic", _G.GunModSettings.automatic)
        end)
    end
end)

-- TAB FARM
local FarmTab = _G.NebulaWindow:Tab({ Title = "FARM", Icon = "package" })
FarmTab:Toggle({ Title = "Auto Minigame", Flag = "auto_minigame", Value = false, Callback = function(v) _G.AutoMinigame = v end })
-- NEBULA HUB | Block Spin - PARTE 2
-- =============================================================

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local Window = _G.NebulaWindow

if not Window then return print("Ejecuta la Parte 1 primero") end

-- TAB SHOP
local ShopTab = Window:Tab({ Title = "SHOP", Icon = "shopping-cart" })
ShopTab:Section({ Title = "AMMO STORE" })

local selectedAmmoType = "Pistol"
ShopTab:Dropdown({
    Title = "Tipo de Bala", Values = {"Pistol", "Rifle", "Shotgun"}, Value = "Pistol", Multi = false,
    Callback = function(v) selectedAmmoType = v end
})

ShopTab:Button({
    Title = "BUY AMMO",
    Callback = function()
        pcall(function()
            local map = workspace:FindFirstChild("Map")
            local crate = map.Tiles.GunShopTile.PatriotWeapons.Interior.Crates["Ammo Crate"].CrateOptions:FindFirstChild(selectedAmmoType)
            _G.NetModule.get("open_crate", crate, "money")
        end)
    end
})

-- TAB VEHICLE
local VehicleTab = Window:Tab({ Title = "VEHICLE", Icon = "car" })
_G.BumpAura = false

VehicleTab:Toggle({
    Title = "Bump Aura", Flag = "BumpAura", Value = false,
    Callback = function(Value)
        _G.BumpAura = Value
        if Value then
            task.spawn(function()
                while _G.BumpAura do
                    task.wait(0.2)
                    local car = _G.VehicleModule.get_car_player_is_in()
                    if car and _G.CharModule then
                        for _, target in _G.CharModule.get_all() do
                            if target ~= LocalPlayer.Character then
                                local hrp = target:FindFirstChild("HumanoidRootPart")
                                local myHrp = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
                                if hrp and myHrp and (myHrp.Position - hrp.Position).Magnitude < 100 then
                                    _G.NetModule.send("run_over", car, target, car.DriverSeat.AssemblyLinearVelocity + Vector3.new(65,65,65))
                                end
                            end
                        end
                    end
                end
            end)
        end
    end
})

-- TAB VISUAL
local VisualTab = Window:Tab({ Title = "VISUAL", Icon = "eye" })
VisualTab:Section({ Title = "ESP Options:" })

_G.ESPNebula_Enabled = false
VisualTab:Toggle({ Title = "ESP NEBULA", Flag = "esp_montana", Value = false, Callback = function(v) _G.ESPNebula_Enabled = v end })

_G.GreenESP_Enabled = true
VisualTab:Toggle({ Title = "Green ESP", Flag = "green_esp", Value = true, Callback = function(v) _G.GreenESP_Enabled = v end })

_G.HideName_Enabled = false
VisualTab:Toggle({ Title = "Hide Name", Flag = "hide_name", Value = false, Callback = function(v) _G.HideName_Enabled = v end })

print("[Nebula Hub] Part 2 Loaded & Attached Successfully!")
