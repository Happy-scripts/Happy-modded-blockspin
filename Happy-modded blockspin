if not game:IsLoaded() then repeat task.wait() until game:IsLoaded() end
if not (game.PlaceId == 104715542330896 or game.PlaceId == 97556409405464) then return end

pcall(function()
    local RS = game:GetService("ReplicatedStorage")
    local TM = require(RS.Modules.Game.UI.TransitionUI)
    local ot = TM.transition
    TM.transition = function(...) return ot and ot(...) end
end)

pcall(function()
    local CC = require(game:GetService("ReplicatedStorage").Modules.Game.CharacterCreator.CharacterCreator)
    if CC.start then CC.start = function() while true do task.wait(1) end end end
end)

pcall(function() if getgenv then getgenv().identifyexecutor = nil end end)

local RS, Http, UIS, Plrs, RSrv, CGui = game:GetService("ReplicatedStorage"), game:GetService("HttpService"), game:GetService("UserInputService"), game:GetService("Players"), game:GetService("RunService"), game:GetService("CoreGui")
local Cam, LP = workspace.CurrentCamera, Plrs.LocalPlayer

local Net = require(RS.Modules.Core.Net)
local RagdollModule = require(RS.Modules.Game.Ragdoll)
local SprintModule = require(RS.Modules.Game.Sprint)

local Char, Hum, Root = LP.Character or LP.CharacterAdded:Wait(), nil, nil
local function updateChar(c) Char = c Hum = c:WaitForChild("Humanoid") Root = c:WaitForChild("HumanoidRootPart") end
updateChar(Char) LP.CharacterAdded:Connect(updateChar)

local function loop(t, f) task.spawn(function() while true do pcall(f) task.wait(t or 0.25) end end) end

local function safeLoad(u)
    local ok, res = pcall(function() return game:HttpGet(u) end)
    if ok and res then local ok2, fn = pcall(loadstring, res) if ok2 and fn then return fn() end end
end

local WindUI = safeLoad("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua")
if not WindUI then return end

local Window = WindUI:CreateWindow({
    Title = "NEBULA HUB | Block Spin", Author = "Nebula.lua", Size = UDim2.fromOffset(480, 360), Theme = "Dark", Transparent = true
})

if CGui:FindFirstChild("NebulaHubButton") then CGui.NebulaHubButton:Destroy() end
local SGui = Instance.new("ScreenGui", CGui) SGui.Name = "NebulaHubButton"
local Btn = Instance.new("ImageButton", SGui) Btn.Size = UDim2.new(0, 45, 0, 45) Btn.Position = UDim2.new(0.5, -22, 0, 10) Btn.Image = "rbxassetid://74826351036046"
Instance.new("UICorner", Btn).CornerRadius = UDim.new(0, 8)
local Stk = Instance.new("UIStroke", Btn) Stk.Color = Color3.fromRGB(0, 229, 255) Stk.Thickness = 2.5

local drag, dStart, sPos, isOpen = false, nil, nil, true
Btn.InputBegan:Connect(function(i) if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then drag, dStart, sPos = true, i.Position, Btn.Position end end)
UIS.InputChanged:Connect(function(i)
    if drag and (i.UserInputType == Enum.UserInputType.MouseMovement or i.UserInputType == Enum.UserInputType.Touch) then
        local delta = i.Position - dStart Btn.Position = UDim2.new(sPos.X.Scale, sPos.X.Offset + delta.X, sPos.Y.Scale, sPos.Y.Offset + delta.Y)
    end
end)
UIS.InputEnded:Connect(function(i)
    if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
        if drag then drag = false isOpen = not isOpen if isOpen then Window:Open() else Window:Close() end end
    end
end)

-- PESTAÑA: GENERAL
local Main = Window:Tab({ Title = "GENERAL", Icon = "globe" })
local SpeedAmount, EnabledSpeed = 35, false

loop(0.25, function()
    if EnabledSpeed and Hum and Hum.Parent then
        Net.send("set_sprinting_1", true)
        if SprintModule and SprintModule.sprinting then SprintModule.sprinting.set(true) end
        Hum:SetAttribute("TargetWalkSpeed", SpeedAmount) Hum.WalkSpeed = SpeedAmount
    end
end)

Main:Toggle({ Title = "Walk Speed", Callback = function(v) EnabledSpeed = v if not v and Hum then Hum.WalkSpeed = 16 Net.send("set_sprinting_1", false) end end })
Main:Slider({ Title = "Speed Value", Step = 1, Value = { Min = 8, Max = 45, Default = 35 }, Callback = function(v) SpeedAmount = v end })

local FlyJump = false
Main:Toggle({ Title = "Fly Jump", Callback = function(v) FlyJump = v end })
UIS.JumpRequest:Connect(function() if FlyJump and Root then Root.AssemblyLinearVelocity = Vector3.new(Root.AssemblyLinearVelocity.X, 55, Root.AssemblyLinearVelocity.Z) end end)

local Spin, SpinAngle = false, 0
Main:Toggle({ Title = "SPIN", Callback = function(v)
    Spin = v
    if v then
        RSrv:BindToRenderStep("Spin", 200, function(dt)
            if Spin and Root then
                SpinAngle = (SpinAngle + 39 * dt) % 6.28
                if Hum then Hum.AutoRotate = false end
                Root.CFrame = CFrame.new(Root.Position) * CFrame.Angles(0, SpinAngle, 0)
            end
        end)
    else RSrv:UnbindFromRenderStep("Spin") if Hum then Hum.AutoRotate = true end end
end })

_G.AutoRecolectMansion = false
Main:Toggle({ Title = "Auto Recolect Mansion", Callback = function(v) _G.AutoRecolectMansion = v end })
loop(0.18, function()
    if not _G.AutoRecolectMansion then return end
    local df = workspace:FindFirstChild("DroppedItems")
    if df and Root then for _, it in ipairs(df:GetChildren()) do if it:FindFirstChild("PickUpZone") then Net.get("pickup_dropped_item", it) end end end
end)

_G.AntiLock = false
Main:Toggle({ Title = "Anti Lock", Callback = function(v) _G.AntiLock = v end })
RSrv.Heartbeat:Connect(function()
    if _G.AntiLock and Root then
        local oldV = Root.Velocity local rV = Vector3.new(math.random(-16000,16000), math.random(-16000,16000), math.random(-16000,16000))
        Root.Velocity = rV Root.AssemblyLinearVelocity = rV RSrv.RenderStepped:Wait() Root.Velocity = oldV
    end
end)

Main:Toggle({ Title = "Anti Ragdoll", Callback = function(v)
    _G.AntiRagdoll = v
    if v then loop(0.2, function() if _G.AntiRagdoll and RagdollModule.is_ragdolling.get() then RagdollModule.is_ragdolling.set(false) Net.send("end_ragdoll_early") end end) end
end })

-- PESTAÑA: COMBAT
local Combat = Window:Tab({ Title = "COMBAT", Icon = "swords" })
_G.SilentAimEnabled = false
Combat:Toggle({ Title = "Silent Aim", Callback = function(v) _G.SilentAimEnabled = v end })

local SendRemote = RS:WaitForChild("Remotes"):WaitForChild("Send")
local oldFire = hookfunction(SendRemote.FireServer, function(self, ...)
    local args = {...}
    if _G.SilentAimEnabled and args[2] == "shoot_gun" then
        local target, short = nil, math.huge
        for _, p in pairs(Plrs:GetPlayers()) do
            if p ~= LP and p.Character and p.Character:FindFirstChild("Head") then
                local d = (p.Character.HumanoidRootPart.Position - Root.Position).Magnitude
                if d < short then short = d target = p.Character.Head end
            end
        end
        if target then
            args[4] = CFrame.lookAt(Cam.CFrame.Position, target.Position)
            args[5] = {{{ Instance = target, Normal = Vector3.new(0,1,0), Position = target.Position, Material = Enum.Material.Plastic, Distance = short }}}
        end
    end
    return oldFire(self, unpack(args))
end)

-- PESTAÑA: FARM
local Farm = Window:Tab({ Title = "FARM", Icon = "package" })
Farm:Toggle({ Title = "Auto Minigame", Callback = function(v)
    if v then loop(0.1, function()
        local sm = require(RS.Modules.Game.Minigames.SliderMinigame)
        if sm and sm.enabled and sm.enabled.get() then sm.needle_pos.set(sm.target_pos.get()) end
    end) end
end })

-- PESTAÑA: VISUAL
local Visual = Window:Tab({ Title = "VISUAL", Icon = "eye" })
Visual:Toggle({ Title = "Green ESP", Callback = function(v)
    for _, p in pairs(Plrs:GetPlayers()) do
        if p ~= LP and p.Character then
            local hl = p.Character:FindFirstChild("NebulaHL") or Instance.new("Highlight", p.Character)
            hl.Name = "NebulaHL" hl.Enabled = v hl.FillColor = Color3.fromRGB(0, 255, 70) hl.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
        end
    end
end })

-- PESTAÑA: PLAYER MODS
local PlayerTab = Window:Tab({ Title = "PLAYER", Icon = "user" })
PlayerTab:Slider({ Title = "Field of View (FOV)", Step = 1, Value = { Min = 70, Max = 120, Default = 70 }, Callback = function(v) Cam.FieldOfView = v end })
PlayerTab:Button({ Title = "Reset Camera", Callback = function() Cam.FieldOfView = 70 end })
