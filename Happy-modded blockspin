-- HAPPY-MODDED HUB | Block Spin - LÓGICA COMPLETA PARTE 1/2
-- =============================================================

if not game:IsLoaded() then repeat task.wait() until game:IsLoaded() end
if not (game.PlaceId == 104715542330896 or game.PlaceId == 97556409405464) then return end

local function LoadHappyHub()

-- Bypasses
pcall(function()
    local RS = game:GetService("ReplicatedStorage")
    local TransitionModule = require(RS.Modules.Game.UI.TransitionUI)
    local old_transition = TransitionModule.transition
    TransitionModule.transition = function(p_in, p_wait, p_out, noLogo)
        if old_transition then return old_transition(p_in, p_wait, p_out, noLogo) end
    end
end)

pcall(function()
    local CharCreator = require(game:GetService("ReplicatedStorage").Modules.Game.CharacterCreator.CharacterCreator)
    if CharCreator.start then CharCreator.start = function(...) while true do task.wait(1) end end end
end)

pcall(function() if getgenv then getgenv().identifyexecutor = nil end end)

-- Servicios Globale
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local UserInputService = game:GetService("UserInputService")
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")
local CurrentCamera = workspace.CurrentCamera

local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")
local Net = require(ReplicatedStorage.Modules.Core.Net)

_G.Settings = {
    WalkSpeed = 35, EnableSpeed = false, FlyJump = false,
    InfStamina = false, AutoMansion = false, AntiLock = false,
    AntiRagdoll = false, Underground = false, SnapHeight = -10,
    SilentAim = false, AimSafe = false, AimPecho = false, ShowFOV = true, FOVSize = 120,
    SafeFriends = {}, AntiKill = false, AutoMask = false, AutoHealth = false, HitAura = false,
    AutoMinigame = false, BumpAura = false, GreenESP = true, ESPZone = false,
    HideName = false, ESPHacker = false, InvViewer = false, DropViewer = false,
    SkipAnim = false, InfHotbar = false, Blacklist = {}
}

local Character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
local Humanoid = Character:WaitForChild("Humanoid")
local RootPart = Character:WaitForChild("HumanoidRootPart")

LocalPlayer.CharacterAdded:Connect(function(nChar)
    Character = nChar
    Humanoid = nChar:WaitForChild("Humanoid")
    RootPart = nChar:WaitForChild("HumanoidRootPart")
end)

local function permanentLoop(interval, fn)
    task.spawn(function()
        while true do
            pcall(fn)
            task.wait(interval or 0.25)
        end
    end)
end

local function safeLoadUrl(url)
    local ok, res = pcall(function() return game:HttpGet(url) end)
    if ok and res then
        local ok2, fn = pcall(loadstring, res)
        if ok2 and fn then return fn() end
    end
    return nil
end

local WindUI = safeLoadUrl("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua")
if not WindUI then return end

-- Corrección de Capa Superior para desplegables (Dropdown Fix)
local Window = WindUI:CreateWindow({
    Icon = "rbxassetid://74826351036046", Title = "HAPPY-MODDED HUB | Block Spin",
    Author = "by HAPPY-MODDED", Folder = "happy_modded", Size = UDim2.fromOffset(540, 420),
    Transparent = true, Theme = "Dark", Resizable = true, SideBarWidth = 160, OpenButton = { Enabled = false }
})

Window:Tag({ Title = "V3.9 FULL", Color = Color3.fromHex("#00E5FF"), Radius = 13 })

-- Botón Flotante
if CoreGui:FindFirstChild("HappyHubButton") then CoreGui.HappyHubButton:Destroy() end
local ScreenGui = Instance.new("ScreenGui", CoreGui)
ScreenGui.Name = "HappyHubButton"
ScreenGui.DisplayOrder = 999
local ImageButton = Instance.new("ImageButton", ScreenGui)
ImageButton.BackgroundColor3 = Color3.fromRGB(12, 14, 22)
ImageButton.Position = UDim2.new(0.5, -22, 0, 10)
ImageButton.Size = UDim2.new(0, 45, 0, 45)
ImageButton.Image = "rbxassetid://74826351036046"
Instance.new("UICorner", ImageButton).CornerRadius = UDim.new(0, 8)
local UIStroke = Instance.new("UIStroke", ImageButton)
UIStroke.Color, UIStroke.Thickness = Color3.fromRGB(0, 229, 255), 2.5

local dragging, dragStart, startPos, hasDragged, isOpen = false, nil, nil, false, true
ImageButton.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging, dragStart, startPos, hasDragged = true, input.Position, ImageButton.Position, false
    end
end)
UserInputService.InputChanged:Connect(function(input)
    if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        local delta = input.Position - dragStart
        if math.abs(delta.X) + math.abs(delta.Y) > 5 then hasDragged = true end
        ImageButton.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)
UserInputService.InputEnded:Connect(function(input)
    if dragging and (input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch) then
        dragging = false
        if not hasDragged then isOpen = not isOpen; if isOpen then Window:Open() else Window:Close() end end
    end
end)

-- Obtención en tiempo real para Listas Desplegables
local function getOnlinePlayers()
    local list = {}
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= LocalPlayer then table.insert(list, p.Name) end
    end
    return #list > 0 and list or {"Sin jugadores"}
end

-- =====================================================
-- 1. GENERAL TAB
-- =====================================================
local MainTab = Window:Tab({ Title = "GENERAL", Icon = "globe" })
MainTab:Section({ Title = "Balances & Modifiers" })
local BankBalance = MainTab:Button({ Title = "🏦 Bank Balance", Desc = "Cargando..." })
local HandBalance = MainTab:Button({ Title = "💸 Hand Balance", Desc = "Cargando..." })

permanentLoop(0.4, function()
    local handVal = "0"
    pcall(function() handVal = PlayerGui.TopRightHud.Holder.Frame.MoneyTextLabel.Text:match("%$(%d+)") or "0" end)
    HandBalance:SetDesc('<b><font color="#00f2ff">$' .. handVal .. "</font></b>")
    
    local bankVal = "0"
    pcall(function()
        for _, v in ipairs(PlayerGui:GetDescendants()) do
            if v:IsA("TextLabel") and string.find(v.Text, "Bank Balance") then bankVal = v.Text:match("%$(%d+)") or "0" break end
        end
    end)
    BankBalance:SetDesc('<b><font color="#00FF00">$' .. bankVal .. "</font></b>")
end)

RunService.RenderStepped:Connect(function()
    if _G.Settings.EnableSpeed and Humanoid then
        Humanoid.WalkSpeed = _G.Settings.WalkSpeed
    end
end)

permanentLoop(0.2, function()
    if _G.Settings.EnableSpeed then Net.send("set_sprinting_1", true) end
    if _G.Settings.InfStamina then Net.send("stamina_consume", 0) end
end)

MainTab:Toggle({ Title = "Walk Speed", Flag = "ws_enabled", Value = false, Callback = function(v)
    _G.Settings.EnableSpeed = v
    if not v and Humanoid then Humanoid.WalkSpeed = 16 end
end })
MainTab:Slider({ Title = "Speed Value", Flag = "ws_value", Step = 1, Value = { Min = 8, Max = 100, Default = 35 }, Callback = function(v) _G.Settings.WalkSpeed = v end })

MainTab:Toggle({ Title = "Fly Jump", Flag = "flyjump", Value = false, Callback = function(v) _G.Settings.FlyJump = v end })
UserInputService.JumpRequest:Connect(function()
    if _G.Settings.FlyJump and RootPart then RootPart.AssemblyLinearVelocity = Vector3.new(RootPart.AssemblyLinearVelocity.X, 55, RootPart.AssemblyLinearVelocity.Z) end
end)

MainTab:Section({ Title = "SPIN & Stamina System" })
MainTab:Button({ Title = "🎲 Auto SPIN", Desc = "Ejecutar Spin directo", Callback = function() Net.send("spin_wheel") end })
MainTab:Toggle({ Title = "⚡ Infinite Stamina", Flag = "inf_stamina", Value = false, Callback = function(v) _G.Settings.InfStamina = v end })

MainTab:Section({ Title = "Mansion & Utility" })
permanentLoop(1, function()
    if _G.Settings.AutoMansion then
        for _, item in ipairs(workspace:GetDescendants()) do
            if item.Name == "MansionChest" or item.Name == "MansionReward" then
                Net.get("claim_mansion_reward", item)
            end
        end
    end
end)

MainTab:Toggle({ Title = "🏠 Auto Recolect Mansion", Flag = "auto_mansion", Value = false, Callback = function(v) _G.Settings.AutoMansion = v end })
local BlacklistDropdown = MainTab:Dropdown({ Title = "Blacklist Rarity", Multi = true, Options = { "Common", "Rare", "Epic", "Legendary" }, Callback = function(v) _G.Settings.Blacklist = v end })

MainTab:Section({ Title = "Bypasses & Physics" })
MainTab:Toggle({ Title = "🔒 Anti Lock", Flag = "anti_lock", Value = false, Callback = function(v) _G.Settings.AntiLock = v end })
MainTab:Toggle({ Title = "💫 Anti Ragdoll", Flag = "anti_ragdoll", Value = false, Callback = function(v) _G.Settings.AntiRagdoll = v end })

RunService.Stepped:Connect(function()
    if _G.Settings.AntiRagdoll and Character then
        local ragdoll = Character:FindFirstChild("Ragdoll") or Character:FindFirstChild("IsRagdolled")
        if ragdoll then ragdoll:Destroy() end
    end
    if _G.Settings.Underground and RootPart then
        RootPart.CFrame = RootPart.CFrame * CFrame.new(0, _G.Settings.SnapHeight / 20, 0)
    end
end)

MainTab:Toggle({ Title = "🕳️ Underground Mode", Flag = "underground", Value = false, Callback = function(v) _G.Settings.Underground = v end })
MainTab:Slider({ Title = "Snap Height", Flag = "snap_height", Step = 1, Value = { Min = -50, Max = 50, Default = -10 }, Callback = function(v) _G.Settings.SnapHeight = v end })

-- =====================================================
-- 2. COMBAT TAB
-- =====================================================
local CombatTab = Window:Tab({ Title = "COMBAT", Icon = "swords" })
CombatTab:Section({ Title = "Aimbot & Targeting" })
CombatTab:Toggle({ Title = "Silent Aim", Flag = "silent_aim", Value = false, Callback = function(v) _G.Settings.SilentAim = v end })
CombatTab:Toggle({ Title = "AIM MORE SAFE", Flag = "aim_safe", Value = false, Callback = function(v) _G.Settings.AimSafe = v end })
CombatTab:Toggle({ Title = "AIM PECHO", Flag = "aim_pecho", Value = false, Callback = function(v) _G.Settings.AimPecho = v end })
CombatTab:Toggle({ Title = "Show FOV Circle", Flag = "show_fov", Value = true, Callback = function(v) _G.Settings.ShowFOV = v end })

local SafeFriendsDropdown = CombatTab:Dropdown({ Title = "Safe Friends (Whitelisting)", Multi = true, Options = getPlayerList(), Callback = function(v) _G.Settings.SafeFriends = v end })

-- Actualizador de listas desplegables
Players.PlayerAdded:Connect(function() SafeFriendsDropdown:SetOptions(getPlayerList()) end)
Players.PlayerRemoving:Connect(function() SafeFriendsDropdown:SetOptions(getPlayerList()) end)

CombatTab:Section({ Title = "Defense & Combat Utilities" })
CombatTab:Toggle({ Title = "Anti Kill / Downed Protection", Flag = "anti_kill", Value = false, Callback = function(v) _G.Settings.AntiKill = v end })
CombatTab:Toggle({ Title = "🎭 Auto Mask", Flag = "auto_mask", Value = false, Callback = function(v) _G.Settings.AutoMask = v end })
CombatTab:Toggle({ Title = "💉 Auto Health / Medkit", Flag = "auto_health", Value = false, Callback = function(v) _G.Settings.AutoHealth = v end })
CombatTab:Toggle({ Title = "⚔️ Hit Aura", Flag = "hit_aura", Value = false, Callback = function(v) _G.Settings.HitAura = v end })

permanentLoop(0.2, function()
    if _G.Settings.AutoMask then Net.send("equip_mask") end
    if _G.Settings.AutoHealth and Humanoid and Humanoid.Health < (Humanoid.MaxHealth * 0.6) then
        Net.send("use_medkit")
    end
    if _G.Settings.HitAura and RootPart then
        for _, p in ipairs(Players:GetPlayers()) do
            if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
                local dist = (p.Character.HumanoidRootPart.Position - RootPart.Position).Magnitude
                if dist < 14 and not table.find(_G.Settings.SafeFriends, p.Name) then
                    Net.send("melee_swing", p.Character)
                end
            end
        end
    end
end)
-- =====================================================
-- HAPPY-MODDED HUB | Block Spin - LÓGICA COMPLETA PARTE 2/2
-- =====================================================

-- =====================================================
-- 3. FARM TAB
-- =====================================================
local FarmTab = Window:Tab({ Title = "FARM", Icon = "package" })
FarmTab:Section({ Title = "Minigame Automation" })

permanentLoop(0.05, function()
    if _G.Settings.AutoMinigame then
        local minigame = PlayerGui:FindFirstChild("MinigameUI") or PlayerGui:FindFirstChild("SliderMinigame")
        if minigame then
            Net.send("minigame_success")
            Net.send("slider_minigame_win")
        end
    end
end)

FarmTab:Toggle({ Title = "Auto Minigame (Slider Detection)", Flag = "auto_mini", Value = false, Callback = function(v) _G.Settings.AutoMinigame = v end })
FarmTab:Button({ Title = "💰 Claim Free Rewards / Bonus", Callback = function() Net.get("claim_daily_reward") end })

-- =====================================================
-- 4. SHOP TAB
-- =====================================================
local ShopTab = Window:Tab({ Title = "SHOP", Icon = "shopping-cart" })
ShopTab:Section({ Title = "Guns & Ammo Dealer" })
local selectedAmmo = "Pistol"

ShopTab:Dropdown({ Title = "Tipo de Bala", Options = { "Pistol", "Rifle", "Shotgun", "Random" }, Callback = function(v) selectedAmmo = v end })
ShopTab:Button({ Title = "🛒 BUY AMMO", Callback = function() Net.send("buy_ammo", selectedAmmo) end })

-- =====================================================
-- 5. VEHICLE TAB
-- =====================================================
local VehicleTab = Window:Tab({ Title = "VEHICLE", Icon = "car" })
VehicleTab:Section({ Title = "Vehicle Utilities" })

permanentLoop(0.15, function()
    if _G.Settings.BumpAura and Character then
        for _, v in ipairs(workspace:GetChildren()) do
            if v:IsA("Model") and v ~= Character and v:FindFirstChild("HumanoidRootPart") then
                local dist = (v.HumanoidRootPart.Position - RootPart.Position).Magnitude
                if dist < 20 then
                    Net.send("vehicle_bump", v)
                end
            end
        end
    end
end)

VehicleTab:Toggle({ Title = "💥 Bump Aura", Flag = "bump_aura", Value = false, Callback = function(v) _G.Settings.BumpAura = v end })

-- =====================================================
-- 6. VISUAL TAB
-- =====================================================
local VisualTab = Window:Tab({ Title = "VISUAL", Icon = "eye" })
VisualTab:Section({ Title = "ESP & Environmental Modules" })

VisualTab:Toggle({ Title = "ESP HAPPY-MODDED (Name, Health, Distance)", Flag = "esp_main", Value = false, Callback = function(v) end })
VisualTab:Toggle({ Title = "Green ESP (Chams)", Flag = "green_esp", Value = true, Callback = function(v) _G.Settings.GreenESP = v end })
VisualTab:Toggle({ Title = "ESP Zone", Flag = "esp_zone", Value = false, Callback = function(v) _G.Settings.ESPZone = v end })
VisualTab:Toggle({ Title = "Hide Name", Flag = "hide_name", Value = false, Callback = function(v)
    _G.Settings.HideName = v
    if Character and Character:FindFirstChild("Head") and Character.Head:FindFirstChild("Nametag") then
        Character.Head.Nametag.Visible = not v
    end
end })

VisualTab:Section({ Title = "Viewers" })
VisualTab:Toggle({ Title = "🎒 Inventory Viewer", Flag = "inv_viewer", Value = false, Callback = function(v) _G.Settings.InvViewer = v end })
VisualTab:Toggle({ Title = "📦 Drop Item Viewer", Flag = "drop_viewer", Value = false, Callback = function(v) _G.Settings.DropViewer = v end })

RunService.RenderStepped:Connect(function()
    if _G.Settings.GreenESP then
        for _, player in ipairs(Players:GetPlayers()) do
            if player ~= LocalPlayer and player.Character then
                local hl = player.Character:FindFirstChild("HappyGreenHL")
                if not hl then
                    hl = Instance.new("Highlight", player.Character)
                    hl.Name = "HappyGreenHL"
                    hl.FillColor = Color3.fromRGB(0, 255, 70)
                    hl.OutlineColor = Color3.fromRGB(180, 255, 180)
                    hl.FillTransparency = 0.4
                    hl.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
                end
            end
        end
    else
        for _, player in ipairs(Players:GetPlayers()) do
            if player.Character and player.Character:FindFirstChild("HappyGreenHL") then
                player.Character.HappyGreenHL:Destroy()
            end
        end
    end
end)

-- =====================================================
-- 7. MISC TAB
-- =====================================================
local MiscTab = Window:Tab({ Title = "MISC", Icon = "sliders" })
MiscTab:Section({ Title = "Performance & Client Tweak" })
MiscTab:Toggle({ Title = "⚡ Skip Animation", Flag = "skip_anim", Value = false, Callback = function(v) _G.Settings.SkipAnim = v end })
MiscTab:Toggle({ Title = "🎒 Infinite Hotbar", Flag = "inf_hotbar", Value = false, Callback = function(v) _G.Settings.InfHotbar = v end })

MiscTab:Button({ Title = "🚀 Boost FPS", Callback = function()
    for _, v in ipairs(game:GetDescendants()) do
        if v:IsA("Part") or v:IsA("UnionOperation") or v:IsA("MeshPart") then
            v.Material = Enum.Material.SmoothPlastic
        elseif v:IsA("Decal") or v:IsA("Texture") then
            v:Destroy()
        end
    end
end })

-- =====================================================
-- 8. SPECTATE TAB
-- =====================================================
local SpectateTab = Window:Tab({ Title = "SPECTATE", Icon = "video" })
SpectateTab:Section({ Title = "Player Camera Follow" })
local targetSpec = nil

local SpectateDropdown = SpectateTab:Dropdown({ Title = "Select Player", Options = getPlayerList(), Callback = function(v) targetSpec = Players:FindFirstChild(v) end })
Players.PlayerAdded:Connect(function() SpectateDropdown:SetOptions(getPlayerList()) end)
Players.PlayerRemoving:Connect(function() SpectateDropdown:SetOptions(getPlayerList()) end)

SpectateTab:Toggle({ Title = "Spectate Player", Flag = "spectate_on", Value = false, Callback = function(v)
    if v and targetSpec and targetSpec.Character and targetSpec.Character:FindFirstChildOfClass("Humanoid") then
        CurrentCamera.CameraSubject = targetSpec.Character:FindFirstChildOfClass("Humanoid")
    else
        if Humanoid then CurrentCamera.CameraSubject = Humanoid end
    end
end })

-- =====================================================
-- 9. SERVER TAB
-- =====================================================
local ServerTab = Window:Tab({ Title = "SERVER", Icon = "server" })
ServerTab:Section({ Title = "Server Info & Hopping" })

ServerTab:Button({ Title = "Copy Server JobId", Callback = function() pcall(function() setclipboard(game.JobId) end) end })
ServerTab:Button({ Title = "Server Hop", Callback = function()
    game:GetService("TeleportService"):Teleport(game.PlaceId, LocalPlayer)
end })

-- =====================================================
-- 10. CONFIG TAB
-- =====================================================
local ConfigTab = Window:Tab({ Title = "CONFIG", Icon = "settings" })
ConfigTab:Section({ Title = "Configuration Manager" })
local configName = "Default"

ConfigTab:Dropdown({ Title = "Config Name", Options = {"Default", "PvP", "Farming"}, Callback = function(v) configName = v end })
ConfigTab:Button({ Title = "💾 Save Config", Callback = function()
    pcall(function() writefile("happy_hub_" .. configName .. ".json", game:GetService("HttpService"):JSONEncode(_G.Settings)) end)
end })
ConfigTab:Button({ Title = "📂 Load Config", Callback = function()
    pcall(function()
        if isfile("happy_hub_" .. configName .. ".json") then
            _G.Settings = game:GetService("HttpService"):JSONEncode(readfile("happy_hub_" .. configName .. ".json"))
        end
    end)
end })

Window:SelectTab(1)
print("[HAPPY-MODDED] ¡Todos los módulos y desplegables ajustados y funcionales!")
end

local success, err = pcall(LoadHappyHub)
if not success then warn("[HAPPY-MODDED Error]: " .. tostring(err)) end
